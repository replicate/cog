# Streaming text generation example using a small open-weight language model.

build:
  gpu: false
  python_version: "3.12"
  python_requirements: requirements.txt

run: "run.py:Runner"

observability:
  traces:
    enabled: true
    sampler: parentbased_always_off
