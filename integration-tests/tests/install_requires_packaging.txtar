# Test that package requirements with specific packaging metadata work (causal-conv1d)

# Build the image
cog build -t $TEST_IMAGE

-- cog.yaml --
build:
  python_version: "3.10"
  python_requirements: requirements.txt
  fast: true
  gpu: true
  cuda: "12.1"
predict: "predict.py:Predictor"

-- requirements.txt --
causal-conv1d==1.5.0.post8
torch==2.5.0

-- predict.py --
from cog import BasePredictor

class Predictor(BasePredictor):
    def predict(self) -> str:
        return "hello"
