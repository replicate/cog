# Test --json flag with inline JSON input
# Source: test_run.py::test_run_json_input

# Build and run prediction with inline JSON
cog build -t $TEST_IMAGE
cog run $TEST_IMAGE --json '{"s": "sackfield"}'
stdout '"status": "succeeded"'
stdout '"output": "hello sackfield"'

-- cog.yaml --
build:
  python_version: "3.12"
run: "run.py:Runner"

-- run.py --
from cog import BaseRunner


class Runner(BaseRunner):
    def run(self, s: str) -> str:
        return "hello " + s
