# Test error when no runner is defined
# Build should fail when cog.yaml has no run field

# Build should fail with error about missing runner
! cog build -t $TEST_IMAGE
stderr 'run'

-- cog.yaml --
build:
  python_version: "3.12"
