# Test -o flag writes string output to file
# Source: test_run.py::test_run_writes_strings_to_files

# Build image
cog build -t $TEST_IMAGE

# Run prediction with -o to write output to file
cog run $TEST_IMAGE -i s=world -o out.txt

# Verify file contents
exec cat out.txt
stdout 'hello world'

-- cog.yaml --
build:
  python_version: "3.12"
run: "run.py:Runner"

-- run.py --
from cog import BaseRunner


class Runner(BaseRunner):
    def run(self, s: str) -> str:
        return "hello " + s
