# Test --json @- reads JSON from stdin
# Source: test_run.py::test_run_json_input_stdin

# Build and run prediction with JSON from stdin
cog build -t $TEST_IMAGE
stdin input.json
cog run $TEST_IMAGE --json @-
stdout '"status": "succeeded"'
stdout '"output": "hello sackfield"'

-- cog.yaml --
build:
  python_version: "3.12"
run: "run.py:Runner"

-- run.py --
from cog import BaseRunner


class Runner(BaseRunner):
    def run(self, s: str) -> str:
        return "hello " + s

-- input.json --
{"s": "sackfield"}
