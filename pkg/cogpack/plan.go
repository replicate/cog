package cogpack

// var registeredProviders []core.Provider

// // RegisterProvider adds p to the global registry. Typical usage is via an
// // init() side-effect in a sub-package:  func init() { cogpack.RegisterProvider(New()) }
// func RegisterProvider(p core.Provider) {
// 	registeredProviders = append(registeredProviders, p)
// }

// // Providers returns a copy of the registered provider slice.
// func Providers() []core.Provider {
// 	cp := make([]core.Provider, len(registeredProviders))
// 	copy(cp, registeredProviders)
// 	return cp
// }
